name: 'weather_forecast'
version: '1.0.0'
profile: 'weather_forecast'

model-paths: ['models']
analysis-paths: ['analysis']
test-paths: ['tests']
seed-paths: ['seeds']
macro-paths: ['macros']
snapshot-paths: ['snapshots']

target-path: 'target'
clean-targets:
  - 'target'
  - 'dbt_packages'

models:
  weather_forecast:
    materialized: table
    +docs:
      show: true

vars:
  timezone: 'America/Bogota'
  start_date: '2024-01-01'
